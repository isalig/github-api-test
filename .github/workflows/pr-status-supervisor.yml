name: Listen dummy-ci status

on:
  workflow_run:
    workflows: ["Dummy CI"]
    types:
      - completed

jobs:
  react:
    runs-on: ubuntu-latest

    steps:
      - name: Report result
        run: |
          echo "${{ toJSON(github.event)}}"
